const qs=(s,p=document)=>p.querySelector(s),qsa=(s,p=document)=>[...p.querySelectorAll(s)];
const glow=qs('.cursor-glow');window.addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'},{passive:true});
const heroBg=qs('.hero-bg');window.addEventListener('scroll',()=>{const y=Math.min(window.scrollY,innerHeight);heroBg.style.transform=`translateY(${y*.14}px) scale(1.06)`},{passive:true});
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in-view')}),{threshold:.16});qsa('.reveal').forEach(el=>observer.observe(el));
const countObserver=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting||e.target.dataset.done)return;e.target.dataset.done='1';const target=Number(e.target.dataset.count),suffix=e.target.dataset.suffix||'',start=performance.now(),duration=1300;const tick=now=>{const p=Math.min(1,(now-start)/duration),ease=1-Math.pow(1-p,3);e.target.textContent=Math.floor(target*ease)+suffix;if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)}),{threshold:.7});qsa('[data-count]').forEach(el=>countObserver.observe(el));
const track=qs('.era-track');let down=false,startX=0,startLeft=0;track.addEventListener('pointerdown',e=>{down=true;startX=e.clientX;startLeft=track.scrollLeft;track.setPointerCapture(e.pointerId);track.classList.add('dragging')});track.addEventListener('pointermove',e=>{if(down)track.scrollLeft=startLeft-(e.clientX-startX)*1.45});const stop=()=>{down=false;track.classList.remove('dragging')};track.addEventListener('pointerup',stop);track.addEventListener('pointercancel',stop);track.addEventListener('wheel',e=>{if(Math.abs(e.deltaY)>Math.abs(e.deltaX)){e.preventDefault();track.scrollLeft+=e.deltaY}},{passive:false});
const rail=qs('.rail-track'),thumb=qs('.rail-thumb'),progress=qs('.rail-progress'),labels=qsa('.rail-label'),sections=qsa('.section[data-label]');let dragging=false,grabOffset=0;const maxScroll=()=>Math.max(1,document.documentElement.scrollHeight-innerHeight);function syncRail(){const ratio=scrollY/maxScroll(),h=rail.clientHeight,y=ratio*h;thumb.style.top=y+'px';progress.style.height=y+'px';const mid=scrollY+innerHeight*.45;let active=sections[0];sections.forEach(s=>{if(s.offsetTop<=mid)active=s});labels.forEach(b=>b.classList.toggle('active',b.dataset.target===active.id))}function scrollFromPointer(clientY){const r=rail.getBoundingClientRect(),y=Math.max(0,Math.min(r.height,clientY-r.top-grabOffset));scrollTo(0,(y/r.height)*maxScroll())}thumb.addEventListener('pointerdown',e=>{dragging=true;document.body.classList.add('dragging');const r=thumb.getBoundingClientRect();grabOffset=e.clientY-(r.top+r.height/2);thumb.setPointerCapture(e.pointerId);e.preventDefault()});thumb.addEventListener('pointermove',e=>{if(dragging)scrollFromPointer(e.clientY)});const end=()=>{dragging=false;grabOffset=0;document.body.classList.remove('dragging')};thumb.addEventListener('pointerup',end);thumb.addEventListener('pointercancel',end);rail.addEventListener('pointerdown',e=>{if(e.target!==thumb){grabOffset=0;scrollFromPointer(e.clientY)}});labels.forEach(b=>b.addEventListener('click',()=>qs('#'+b.dataset.target).scrollIntoView({behavior:'smooth'})));addEventListener('scroll',syncRail,{passive:true});addEventListener('resize',syncRail);syncRail();
const sound=qs('.sound-btn');let audioCtx,oscillator,gain;sound.addEventListener('click',async()=>{const on=sound.getAttribute('aria-pressed')==='true';if(on){sound.setAttribute('aria-pressed','false');if(gain)gain.gain.exponentialRampToValueAtTime(.0001,audioCtx.currentTime+.4);return}sound.setAttribute('aria-pressed','true');if(!audioCtx){audioCtx=new(window.AudioContext||window.webkitAudioContext)();oscillator=audioCtx.createOscillator();gain=audioCtx.createGain();oscillator.type='sine';oscillator.frequency.value=47;gain.gain.value=.0001;oscillator.connect(gain).connect(audioCtx.destination);oscillator.start()}await audioCtx.resume();gain.gain.exponentialRampToValueAtTime(.018,audioCtx.currentTime+.5)});
